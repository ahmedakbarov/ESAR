# Place operator-supplied CA certificates here as *.crt files (e.g. ad-ldaps-ca.crt).
# They are copied into the API/Workers images and trusted via update-ca-certificates.
# Actual certificates are git-ignored; only this placeholder is committed.
